# Database
# For local development, you can use PostgreSQL or SQLite
# For production (Vercel), you MUST use PostgreSQL

# PostgreSQL (Recommended for both dev and production)
DATABASE_URL=postgresql://user:password@localhost:5432/ecommerce?schema=public

# SQLite (Only for local dev - does NOT work on Vercel)
# DATABASE_URL=file:./dev.db

# Stripe (test)
STRIPE_PUBLIC_KEY=pk_test_XXXXXXXX
STRIPE_SECRET_KEY=sk_test_XXXXXXXX

# M-Pesa Daraja (sandbox)
MPESA_CONSUMER_KEY=your_consumer_key
MPESA_CONSUMER_SECRET=your_consumer_secret
MPESA_SHORTCODE=174379
MPESA_PASSKEY=your_passkey
MPESA_ENV=sandbox

# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000
